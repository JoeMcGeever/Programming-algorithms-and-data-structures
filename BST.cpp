#include <iostream>
#include <string>
#include <ifstream>
using namespace std;

//alphabetically store using ascii values (capital letters should not affect)

struct Node
{
    int value, frequency = 0;
    Node* leftChild;
    Node* rightChild;
}

vector<int> constructTree()
{
    ifstream textFile.txt;
    vector<string> binaryTree;
    textFile.open("textFile.txt");
    while(file>>word)
    {
        
    }
    textFile.close();
    
}

int main()
{
    
}